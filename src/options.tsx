import * as React from "react";
import * as ReactDOM from "react-dom";

const OptionsApp = (): React.ReactElement => {
  return <div>Hello</div>;
};

ReactDOM.render(<OptionsApp />, document.getElementById("root"));
